#!/bin/sh

update() {

    local stashed=0
    if git status | grep -q [a-z]; then
        git stash save hupdate
        stashed=1
    fi

    git pull

    if [ $stashed -eq 1 ]; then
        git stash pop -q
    fi

    hupdate-work
}

main() {

    (
	cd $HOME
	update
    )
}

main "$@"
