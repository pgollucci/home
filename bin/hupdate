#!/bin/sh

#### Options
Help=0

while getopts h o; do
  case "$o" in
    h) echo "hupdate [-h]"; exit 0;;
  esac
done

for m in $HOME/.dotfiles; do
  if [ -d $m ]; then
    cd $m
    git fetch upstream
    git merge upstream/master
    git push
   fi
done

echo "Done"
