#!/bin/sh

#### Options
Help=0

while getopts h o; do
  case "$o" in
    h) echo "hinit [-h]"; exit 0;;
  esac
done

for m in $HOME $HOME/.dotfiles $HOME/.oh-my-zsh $HOME/.rbenv; do
  cd $m
  git pull origin
done

if [ -d $HOME/.private ]; then
  cd $HOME/.private
  git pull origin
fi

if [ -d $HOME/alg4s ]; then
  cd $HOME/alg4s
  git pull orgin
fi

echo "Done"
